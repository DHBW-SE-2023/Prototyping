[tool.poetry]
name = "python-fastapi"
version = "0.1.0"
description = ""
authors = ["MaxAlberti <maxalberti@gmx.de>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
